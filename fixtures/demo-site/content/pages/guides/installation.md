---
title: Installation
description: The second page of an authored sequence, even though it sorts first by path.
order: 5
---

`installation` sorts before `introduction` alphabetically. This page still
comes second, because `order: 5` puts it after `order: 4`.

Remove the key from both files and the pair reverts to path order.
